import logging, numpy as np, rpxdock as rp

log = logging.getLogger(__name__)

def filter_sscount(xforms, body, scores=None, min_helix_length=4, min_sheet_lengt=3, min_loop_length=1, min_element_resis=1, sstype="EHL", **kw):
    return None
